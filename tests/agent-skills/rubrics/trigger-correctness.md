# Rubric: Trigger Correctness

A good response:
- clearly uses the intended skill behavior for the case
- does not drift into generic coding help when the case requires guardrails
- does not trigger the wrong mode (`coding-guard` vs `refactor-coach`)

A weak response:
- ignores the requested architecture posture
- responds as ordinary implementation help without guardrail outputs
